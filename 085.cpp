#include <stdio.h>
#include <string.h>
#define MAX 101 ;
struct work{
	char[15] ;
	int age ;
	char[15] ;

} ;
int input(work *a)
{
	int i=0, n=0;
	while(1)
	{
		scanf("%s",a[i].name) ;
		if(!strcmp(a,"#")) 
			break ;
		else
			i++ ;
			n++ ;
		return n ;
	}
}

void search(work *b, char *c, n)
{
	
}

